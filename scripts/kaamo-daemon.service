[Unit]
Description=Kaamo Daemon
After=network.target docker.service

[Service]
Type=simple
WorkingDirectory=%h/.kaamo
ExecStart=/usr/bin/env uvicorn kaamo.daemon.server:app --host 127.0.0.1 --port 8080
Restart=always

[Install]
WantedBy=multi-user.target

